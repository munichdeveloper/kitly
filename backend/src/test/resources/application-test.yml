spring:
  application:
    name: kitly-backend-test
  
  datasource:
    # Testcontainers will override these
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    baseline-on-migrate: true
    # db/testdata provides a versioned Stripe test-mode fixture (see
    # db/testdata/R__seed_stripe_test_mode.sql) on top of the regular
    # schema migrations, so every test/CI run starts from a known Stripe
    # configuration instead of a manually maintained platform_settings row.
    locations: classpath:db/migration,classpath:db/testdata
    clean-disabled: false

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: false

jwt:
  secret: test-secret-key-for-jwt-token-signing
  expiration: 86400000
  session:
    secret: test-session-secret-key-for-jwt-token-signing
    expiration: 900000

logging:
  level:
    org.springframework: WARN
    org.hibernate: WARN
    org.flywaydb: INFO
    com.kitly.saas: INFO
    org.testcontainers: INFO
