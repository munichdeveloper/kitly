# Rename to .env and fill in the values
CORS_ALLOWED_ORIGINS=https://landingpage.example,https://app.example.com
POSTGRES_PASSWORD=postgrespassword

# STRIPE
# All Stripe configuration is now managed dynamically via Platform Settings
# Configure via Platform Admin UI at /admin/platform or via SQL:
#
# -- Set Stripe mode (test or live)
# INSERT INTO platform_settings (setting_key, setting_value, type, description) VALUES
#   ('stripe.mode', 'test', 'STRING', 'Stripe API mode: test or live');
#
# -- Configure API credentials
# INSERT INTO platform_settings (setting_key, setting_value, type, description, is_encrypted) VALUES
#   ('stripe.test.api_key', 'sk_test_...', 'STRING', 'Stripe Test API Key', true),
#   ('stripe.test.webhook_secret', 'whsec_...', 'STRING', 'Stripe Test Webhook Secret', true);
#
# -- Configure plan prices
# INSERT INTO platform_settings (setting_key, setting_value, type, description) VALUES
#   ('stripe.test.plan.STARTER', 'price_1234...', 'STRING', 'Stripe Test Price ID for STARTER plan'),
#   ('stripe.test.plan.BUSINESS', 'price_5678...', 'STRING', 'Stripe Test Price ID for BUSINESS plan'),
#   ('stripe.test.plan.ENTERPRISE', 'price_9012...', 'STRING', 'Stripe Test Price ID for ENTERPRISE plan');

